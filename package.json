{
  "name": "ficsitprints",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node server.js",
    "deploy:cloudflare": "wrangler pages deploy . --project-name ficsitprints"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
